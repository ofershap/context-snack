import { closeCountdownClientJs } from '../closeCountdownUi';

export function feedWebviewClientJs(gamesEnabled = true): string {
    const gamesFlag = gamesEnabled ? 'true' : 'false';
    return (
        'const GAMES_ENABLED = ' + gamesFlag + ';\n' +
        "        const vscode = acquireVsCodeApi();\n        const stage = document.getElementById('feed-stage');\n        const progressEl = document.getElementById('progress');\n        const openBtn = document.getElementById('openBtn');\n\n        const BODY_MAX = 420;\n\n        let cards = [];\n        let index = 0;\n        let readSet = new Set();\n\n        function notifyReady() {\n            vscode.postMessage({ command: 'ready' });\n            if (window.__PREVIEW_CARDS__ && window.__PREVIEW_CARDS__.length) {\n                cards = window.__PREVIEW_CARDS__;\n                index = 0;\n                readSet = new Set();\n                render();\n            }\n        }\n\n        if (document.readyState === 'loading') {\n            document.addEventListener('DOMContentLoaded', notifyReady);\n        } else {\n            notifyReady();\n        }\n\n        window.addEventListener('message', event => {\n            const message = event.data;\n            if (message.command === 'loadCards') {\n                cancelCloseCountdown();\n                cards = message.cards || [];\n                index = 0;\n                readSet = new Set();\n                render();\n            } else if (message.command === 'startCloseCountdown') {\n                startCloseCountdown();\n            } else if (message.command === 'cancelCloseCountdown') {\n                cancelCloseCountdown();\n            }\n        });\n\n        " +
        closeCountdownClientJs() +
        '\n\n        function escapeHtml(text) {\n            return String(text)\n                .replace(/&/g, \'&amp;\')\n                .replace(/</g, \'&lt;\')\n                .replace(/>/g, \'&gt;\')\n                .replace(/"/g, \'&quot;\')\n                .replace(/\'/g, \'&#39;\');\n        }\n\n        function categoryClass(category) {\n            switch (category) {\n                case \'cursor\': case \'ai\': case \'dev\': case \'social\': case \'fun\':\n                    return \'cat-\' + category;\n                default:\n                    return \'cat-dev\';\n            }\n        }\n\n        function truncateBody(text) {\n            const trimmed = text.trim();\n            if (trimmed.length <= BODY_MAX) return trimmed;\n            const slice = trimmed.slice(0, BODY_MAX);\n            const marks = [\'. \', \'! \', \'? \'];\n            let best = -1;\n            for (const mark of marks) {\n                const i = slice.lastIndexOf(mark);\n                if (i >= BODY_MAX * 0.45 && i > best) best = i;\n            }\n            if (best >= 0) return slice.slice(0, best + 1).trim();\n            const space = slice.lastIndexOf(\' \');\n            if (space >= BODY_MAX * 0.55) return slice.slice(0, space).trimEnd() + \'…\';\n            return slice.trimEnd() + \'…\';\n        }\n\n        function cardBody(card) {\n            const text = ((card.summary || card.subtitle) || \'\').trim();\n            if (text) return truncateBody(text);\n            return \'\';\n        }\n\n        function renderChrome() {\n            return \'<div class="card-chrome">\' +\n                \'<div class="card-chrome-text"><div class="card-chrome-title">🍿 Context Snack</div><div class="card-chrome-subtitle">while your agent works</div></div>\' +\n                \'<div class="card-chrome-actions">\' +\n                    \'<button type="button" class="chrome-icon-btn" id="muteBtn" title="Do not pop Context Snack for the current agent conversation" aria-label="Do not pop Context Snack for the current agent conversation" onclick="muteChat()">🔕</button>\' +\n                    (GAMES_ENABLED ? \'<button type="button" class="chrome-icon-btn" id="gameBtn" title="News break - play a game!" aria-label="News break - play a game!" onclick="requestGame()">🎮</button>\' : \'\') +\n                    \'<button type="button" class="chrome-icon-btn" id="closeBtn" title="Close" aria-label="Close" onclick="closeFeed()">✕</button>\' +\n                \'</div></div>\';\n        }\n\nfunction renderCardInnerHtml(card) {\n            const body = cardBody(card);\n            const badge = \'<div class="card-header"><span class="badge \' + categoryClass(card.category) + \'">\' +\n                escapeHtml(card.emoji) + \' \' + escapeHtml(card.source) + \'</span></div>\';\n            const title = \'<h2 class="card-title">\' + escapeHtml(card.title) + \'</h2>\';\n            const imageHtml = card.image\n                ? \'<div class="card-image-wrap"><img class="card-image" src="\' + escapeHtml(card.image) + \'" alt="" loading="lazy" /></div>\'\n                : \'\';\n            const bodyHtml = body ? \'<p class="card-body">\' + escapeHtml(body) + \'</p>\' : \'\';\n            const footerParts = [\'<span class="card-meta">\' + (card.meta ? escapeHtml(card.meta) : \'\') + \'</span>\'];\n            if (card.url) footerParts.push(\'<span class="card-open">Open ↗</span>\');\n            const footer = \'<div class="card-footer">\' + footerParts.join(\'\') + \'</div>\';\n            return badge + title + imageHtml + bodyHtml + footer;\n        }\n\n        function markRead(i) {\n            if (i >= 0 && i < cards.length && !readSet.has(i)) {\n                readSet.add(i);\n                vscode.postMessage({\n                    command: \'cardRead\',\n                    id: cards[i].id,\n                    index: i\n                });\n            }\n        }\n\n        function render() {\n            stage.innerHTML = \'\';\n\n            if (cards.length === 0) {\n                stage.innerHTML = \'<div class="card" style="justify-content:center;text-align:center;">\' +\n                    renderChrome() +\n                    \'<div style="font-size:36px;margin-bottom:10px;">🛰️</div>\' +\n                    \'<h2 class="card-title">No snacks cached yet</h2>\' +\n                    \'<p class="card-body" style="flex:0;">Feed refreshes in the background — check back soon.</p>\' +\n                    \'</div>\';\n                progressEl.textContent = \'0 / 0\';\n                openBtn.disabled = true;\n                return;\n            }\n\n            if (index >= cards.length) {\n                stage.innerHTML = \'<div class="card" id="endCard">\' +\n                    renderChrome() +\n                    \'<div class="emoji">🎉</div>\' +\n                    \'<h2 class="card-title">All caught up</h2>\' +\n                    \'<p class="card-body">Read \' + readSet.size + \' cards this round.</p>\' +\n                    \'</div>\';\n                progressEl.textContent = cards.length + \' / \' + cards.length;\n                openBtn.disabled = true;\n                markRead(cards.length - 1);\n                return;\n            }\n\n            [2, 1, 0].forEach(offset => {\n                const i = index + offset;\n                if (i >= cards.length) return;\n                const card = cards[i];\n                const el = document.createElement(\'div\');\n                el.className = \'card\' + (card.image ? \' has-image\' : \'\');\n                el.style.zIndex = String(10 - offset);\n                el.style.transform = offset === 0\n                    ? \'translateY(0) scale(1)\'\n                    : \'translateY(\' + (offset * 12) + \'px) scale(\' + (1 - offset * 0.045) + \')\';\n                el.style.opacity = offset === 2 ? \'0.5\' : offset === 1 ? \'0.78\' : \'1\';\n\n                el.innerHTML = (offset === 0 ? renderChrome() : \'\') + renderCardInnerHtml(card);\n                const img = el.querySelector(\'.card-image\');\n                if (img) {\n                    img.addEventListener(\'error\', () => {\n                        const wrap = el.querySelector(\'.card-image-wrap\');\n                        if (wrap) wrap.remove();\n                        el.classList.remove(\'has-image\');\n                    });\n                }\n\n                if (offset === 0) {\n                    el.innerHTML +=\n                        \'<div class="indicator indicator-next">NEXT</div>\' +\n                        \'<div class="indicator indicator-prev">PREV</div>\' +\n                        \'<div class="indicator indicator-open">OPEN</div>\';\n                    attachDrag(el);\n                }\n\n                stage.appendChild(el);\n            });\n\n            progressEl.textContent = (index + 1) + \' / \' + cards.length;\n            openBtn.disabled = !cards[index].url;\n            markRead(index);\n        }\n\n        function attachDrag(el) {\n            let startX = 0, startY = 0, dx = 0, dy = 0, dragging = false;\n            const nextIndicator = () => el.querySelector(\'.indicator-next\');\n            const prevIndicator = () => el.querySelector(\'.indicator-prev\');\n            const openIndicator = () => el.querySelector(\'.indicator-open\');\n            const THRESHOLD = 90;\n            const MAX_ROTATE = 14;\n\n            el.addEventListener(\'pointerdown\', e => {\n                if (e.target.closest(\'button\')) return;\n                dragging = true;\n                startX = e.clientX;\n                startY = e.clientY;\n                el.setPointerCapture(e.pointerId);\n            });\n\n            el.addEventListener(\'pointermove\', e => {\n                if (!dragging) return;\n                dx = e.clientX - startX;\n                dy = e.clientY - startY;\n                const rotate = Math.max(-MAX_ROTATE, Math.min(MAX_ROTATE, dx / 14));\n                el.style.transform = \'translate(\' + dx + \'px, \' + dy + \'px) rotate(\' + rotate + \'deg)\';\n\n                nextIndicator().style.opacity = dx > 20 ? Math.min(1, dx / THRESHOLD) : 0;\n                prevIndicator().style.opacity = dx < -20 ? Math.min(1, -dx / THRESHOLD) : 0;\n                openIndicator().style.opacity = (dy < -20 && Math.abs(dy) > Math.abs(dx)) ? Math.min(1, -dy / THRESHOLD) : 0;\n            });\n\n            const finishDrag = () => {\n                if (!dragging) return;\n                dragging = false;\n\n                if (dy < -THRESHOLD && Math.abs(dy) > Math.abs(dx)) {\n                    el.style.transition = \'transform 0.2s ease-out\';\n                    el.style.transform = \'translateY(-36px)\';\n                    openCurrent();\n                    setTimeout(() => { el.style.transition = \'\'; el.style.transform = \'translateY(0) scale(1)\'; }, 200);\n                } else if (dx > THRESHOLD) {\n                    flyOut(el, 1, goNext);\n                } else if (dx < -THRESHOLD) {\n                    flyOut(el, -1, goPrev);\n                } else {\n                    el.style.transition = \'transform 0.22s ease-out\';\n                    el.style.transform = \'translateY(0) scale(1)\';\n                    setTimeout(() => { el.style.transition = \'\'; }, 220);\n                }\n                dx = 0; dy = 0;\n            };\n\n            el.addEventListener(\'pointerup\', finishDrag);\n            el.addEventListener(\'pointercancel\', finishDrag);\n        }\n\n        function flyOut(el, direction, then) {\n            el.style.transition = \'transform 0.22s ease-out, opacity 0.22s ease-out\';\n            el.style.transform = \'translate(\' + (direction * 480) + \'px, -24px) rotate(\' + (direction * 12) + \'deg)\';\n            el.style.opacity = \'0\';\n            setTimeout(then, 180);\n        }\n\n        function goNext() {\n            if (index < cards.length) {\n                index++;\n                render();\n            }\n        }\n\n        function goPrev() {\n            if (index > 0) {\n                index--;\n                render();\n            }\n        }\n\n        function openCurrent() {\n            const card = cards[index];\n            if (card && card.url) {\n                vscode.postMessage({ command: \'openLink\', url: card.url });\n            }\n        }\n\n        function requestGame() {\n            vscode.postMessage({ command: \'requestGame\' });\n        }\n\n        function muteChat() {\n            const btn = document.getElementById(\'muteBtn\');\n            if (btn) {\n                btn.textContent = \'🔇\';\n                btn.title = \'Context Snack muted for the current agent conversation\';\n                btn.setAttribute(\'aria-label\', \'Context Snack muted for the current agent conversation\');\n                btn.classList.add(\'is-muted\');\n            }\n            vscode.postMessage({ command: \'muteChat\' });\n        }\n\n        function closeFeed() {\n            vscode.postMessage({ command: \'closeFeed\' });\n        }\n\n        document.addEventListener(\'keydown\', e => {\n            if (e.key === \'ArrowRight\') goNext();\n            else if (e.key === \'ArrowLeft\') goPrev();\n            else if (e.key === \'ArrowUp\' || e.key === \'Enter\') openCurrent();\n            else if (e.key === \'Escape\') closeFeed();\n        });'
    );
}
